<Window x:Class="DMAM.Application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid Margin="10,10,10,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                  AutoGenerateColumns="False" ItemsSource="{Binding Volumes}"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Drive" Width="60" Binding="{Binding DriveLetter}" />
                <DataGridTextColumn Header="Label" Width="150" Binding="{Binding Label}" />
                <DataGridTextColumn Header="Type" Width="180" Binding="{Binding VolumeType}" />
                <DataGridTextColumn Header="CD Audio" Width="70" Binding="{Binding ContainsCDAudio}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
